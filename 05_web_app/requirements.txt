# Dépendances de l'application web (déploiement Streamlit Community Cloud).
#
# Les bibliothèques de calcul sont ÉPINGLÉES aux versions ayant servi à
# entraîner et sérialiser le modèle. Un pickle scikit-learn / XGBoost ne se
# recharge de façon fiable qu'avec les mêmes versions : un simple « >= »
# laisserait pip installer une version plus récente dont la structure interne
# diffère, ce qui casse le chargement du modèle.

streamlit>=1.57

# --- Versions figées : doivent correspondre à l'environnement d'entraînement ---
scikit-learn==1.6.1
numpy==2.1.3
scipy==1.15.3
xgboost==3.3.0
joblib==1.4.2
pandas==2.2.3
shap==0.52.0

# --- Rapports PDF et donnees de reference ---
fpdf2>=2.7
pyarrow>=15.0
